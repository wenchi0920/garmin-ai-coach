
# fit 檔案規則

1. 檔案名稱必須要 一致化 修改成 20240913-235534+0800.fit, 只要不是 20240913-235534+0800.fit 的格式 ，就讀取 *.fit 然後找出 裡面的活動時間 ,要自動偵測 檔案 時區 問題, 用活動時間 並把檔名 改為 20240913-235534+0800.fit 的檔案名稱 格式
2. 非跑步的紀錄要刪除檔案
3. 獨立一隻 filter_runner_fit.py 處理檔案

 python3 filter_runner_fit.py 指定資料夾or指定檔案

